package ObserverPattern.MoreComplexity;

public class EmailMsgListener implements EventListener{
    @Override
    public void update(Event eventType) {
        // actually email the customers
    }
}
